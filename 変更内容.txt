Cameraクラスのログに対応（LogVersion=50） 読み取り成功！

・LogFileSize = WorkAddress + 1024;	//実質のサイズを保存用に記録しておく
  だとファイルサイズが大きくなっていくので，

・fileOpen_v50.cs 内で
  WorkAddress -= 512;		//ファイルサイズの微調整（対処療法）
  を最後に実行して辻褄を合わせている。（エレガントな解決方法ではない）
